apiVersion: v1
kind: Service
metadata:
  name: {{ include "forgekeeper.fullname" . }}
  labels:
    {{- include "forgekeeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "forgekeeper.selectorLabels" . | nindent 4 }}
