apiVersion: v1
kind: Service
metadata:
  name: forgekeeper
  namespace: forgekeeper
  labels:
    app.kubernetes.io/name: forgekeeper
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: forgekeeper
    app.kubernetes.io/component: api
